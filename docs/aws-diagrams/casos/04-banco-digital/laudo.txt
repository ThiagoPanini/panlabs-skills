════ o arco inteiro — os sete passos ════

── passo 4 · a revisão de lacunas (sobre a projeção lógica) ──

  Banco digital · segregação regulatória · vista lógica  (12 nós, teto 3)
    ⚠ spof                         consolidar             sem par, e é o único caminho de "regulador" até 7 outros componentes

    mudas (a regra não tem sujeito neste modelo — não é o mesmo que passar):
      · single-az                    nenhuma subnet declara `az`: o modelo não fala de zona
      · egress-sem-controle          nenhuma subnet privada com conteúdo: não há de onde sair
      · dado-em-subnet-publica       nenhuma subnet pública declarada
      · cross-account-sem-confianca  0 conta(s): não há travessia possível
      · assincrono-sem-dlq           nenhuma aresta sai de fila, tópico ou barramento: não há caminho assíncrono

    1 achado(s) · teto ⌈12÷4⌉ = 3 · dentro

── passo 2 · o laudo das 62 sobre a vista lógica ──
  ⚠ A6.4  Alinhamento a grid
        só 46% alinhados (mínimo 70%)
        · risco não compartilha eixo com nenhum outro nó
        · n-risco não compartilha eixo com nenhum outro nó
        · trilha não compartilha eixo com nenhum outro nó
        · n-escrita não compartilha eixo com nenhum outro nó
        · correntista não compartilha eixo com nenhum outro nó
        · … e mais 1

  62 checagens: 39 ok · 12 aviso · 6 falha · 3 inaplicável · 2 do render

✗ 1/1 diagrama(s) com falha

── passo 6 · o laudo das 62 sobre a vista TÉCNICA ──
        · duas arestas saem de "consolidar" a 0° uma da outra (piso 15°)
        · duas arestas saem de "correntista" a 0° uma da outra (piso 15°)
  ⚠ A6.3  Razão de aspecto (Asp)
        Asp = 0.238 < 0.499 (Q1): o desenho é uma faixa muito alongada; a razão do desenho difere da do canvas em 60% (tolerância 20%): sobram faixas vazias
        · Asp = 0.238 < 0.499 (Q1): o desenho é uma faixa muito alongada
        · a razão do desenho difere da do canvas em 60% (tolerância 20%): sobram faixas vazias
  ⚠ A6.5  Preservação de vizinhança (NP) / Stress (KSM)
        NP = 0.297 < 0.341 (Q1) — mas ver a ressalva: aqui a posição vem dos grupos, não do grafo
        · NP = 0.297 < 0.341 (Q1)

  62 checagens: 33 ok · 12 aviso · 14 falha · 1 inaplicável · 2 do render
  ✗ 1 falha(s) SEMÂNTICA(s) — o desenho afirma o que o modelo nega

✗ 1/1 diagrama(s) com falha

── o portão de veracidade sobre a vista técnica ──

✗ a página "banco-segregacao-tecnica": geometria reprovada no portão "veracidade"
    · A5.5 Aresta atravessando fronteira espúria (o desenho afirma o que o modelo nega): 1 travessia(s) de fronteira alheia — tolerância é zero
    ·     · a aresta "a-envia" (consolidar→regulador) atravessa o grupo "f-transacional", de onde não sai nem para onde vai — o desenho sugere um caminho de rede que não existe
    · A3.2 Sobreposição rótulo–rótulo: 1 colisão(ões) de rótulo
    ·     · os rótulos de movimentar ("ECS · motor de movimentação") e a-abre ("abre a conta") se cruzam em 14 px²
    · A3.4 Sobreposição rótulo–aresta: 
    ·     · a aresta "a-trilha-t" passa por cima do rótulo de correntista ("Correntistas")
(código de saída: 0)
