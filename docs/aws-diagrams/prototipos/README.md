# ⚠️ Protótipos descartáveis — não é a skill

Código e prosa de uma pergunta só, do ticket
[#15 · Protocolo de sabatina](https://github.com/ThiagoPanini/panlabs-skills/issues/15).
**Nada aqui vira produção.** O que sobrevive é a *decisão*, que fica na resolução do ticket;
estes arquivos ficam como fonte primária.

| Arquivo | O que é |
|---|---|
| `sabatina.html` | Demo de lógica, um arquivo só — abre com duplo clique, sem instalar nada. O módulo `Sabatina` é puro (zero DOM) e é a parte liftável. Roteiros guiados nos casos difíceis de julgar no papel. |
| `transcricao-cenario-ficticio.md` | O protocolo rodado ponta a ponta contra um cenário fictício, com as candidatas propostas. É o entregável que o ticket pediu. |

**Pergunta que os dois respondem:** a sabatina para sozinha na hora certa, e impede que uma
arquitetura inventada seja desenhada como se existisse?

> O repositório ainda não tem commit algum (`main` vazia), então não há branch `prototype/*`
> de descarte — mesmo caso da pesquisa do [#8](https://github.com/ThiagoPanini/panlabs-skills/issues/8).
> Ao inicializar o repo, mover para uma branch descartável e deixar o ponteiro no ticket.
